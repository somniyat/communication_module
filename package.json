{
  "name": "communication-module",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-channel communication dispatch system (email, sms, whatsapp, push, in-app).",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm:dev:backend\" \"npm:dev:frontend\"",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build && node backend/scripts/build-client.js",
    "start": "NODE_ENV=production node backend/src/server.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
